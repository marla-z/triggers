pipeline {
    agent { label "runner" }
 
    stages {
        stage('Перевірка Jenkinsfile') {
            steps {
                script {
                    sh "env"
                }
            }
        }

        // Додай інші етапи тут
    }
}
